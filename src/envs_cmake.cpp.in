#include "envs_cmake.h"

namespace LIBRPA
{

namespace envs
{

const char * source_dir = "${PROJECT_SOURCE_DIR}/src";

const char * git_hash = "${GIT_SHA1}";

const char * git_ref = "${GIT_REF}";

const char * cxx_compiler = "${CMAKE_CXX_COMPILER}";

const char * fortran_compiler = "${CMAKE_Fortran_COMPILER}";

const char * cxx_compiler_flags = "${CMAKE_CXX_FLAGS}";

const char * fortran_compiler_flags = "${CMAKE_Fortran_FLAGS}";

} /* end of namespace envs */
} /* end of namespace LIBRPA */
