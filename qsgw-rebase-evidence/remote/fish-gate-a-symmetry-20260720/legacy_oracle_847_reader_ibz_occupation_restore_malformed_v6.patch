diff --git a/driver/read_data.cpp b/driver/read_data.cpp
--- a/driver/read_data.cpp
+++ b/driver/read_data.cpp
@@ -2619,6 +2619,5 @@ static int handle_sinvS_file(const string &file_path,
                              map<Vector3_Order<double>, ComplexMatrix> &sinvS, bool binary)
 {
-    std::vector<size_t> shrinked_mu;
     ifstream infile;
     int n_irk_points_local;
 
@@ -2658,10 +2657,6 @@ static int handle_sinvS_file(const string &file_path,
             bcol--;
             ecol--;
             iq--;
-            if (iq == 0)
-            {
-                shrinked_mu.emplace_back(erow - brow + 1);
-            }
             Vector3_Order<double> qvec = resolve_abacus_file_qvec(iq, n_irk_points, file_path);
 
             if (!sinvS.count(qvec))
@@ -2705,10 +2700,6 @@ static int handle_sinvS_file(const string &file_path,
             int bcol = stoi(begin_col) - 1;
             int ecol = stoi(end_col) - 1;
             int iq = stoi(q_num) - 1;
-            if (iq == 0)
-            {
-                shrinked_mu.emplace_back(erow - brow + 1);
-            }
 
             // skip empty coulumb_file
             if ((erow - brow < 0) || (ecol - bcol < 0) || iq < 0 || iq > klist.size()) return 4;
@@ -2730,29 +2721,15 @@ static int handle_sinvS_file(const string &file_path,
             }
         }
     }
-    // shrinked_mu: {59, 59, 80, 80}
-    size_t n = shrinked_mu.size() / atom_mu.size();
-    for (auto &Imu : atom_mu)
-    {
-        const auto I = Imu.first;
-        size_t mu_mod = 0;
-        for (int imu = I * n; imu < (I + 1) * n; imu++)
-        {
-            mu_mod += shrinked_mu.at(imu);
-        }
-        mu_mod = mu_mod / n;
-        // we can also use larger abfs for test or interested
-        // assert(mu_mod <= Imu.second);
-        atom_mu[I] = mu_mod;
-    }
     // reset
-    LIBRPA::atomic_basis_abf.set(atom_mu);
-    atom_mu_part_range.resize(atom_mu.size());
+    atom_mu = atom_mu_s;
+    LIBRPA::atomic_basis_abf.set(atom_mu_s);
+    atom_mu_part_range.resize(atom_mu_s.size());
     atom_mu_part_range[0] = 0;
-    for (int I = 1; I != atom_mu.size(); I++)
-        atom_mu_part_range[I] = atom_mu.at(I - 1) + atom_mu_part_range[I - 1];
+    for (int I = 1; I != atom_mu_s.size(); I++)
+        atom_mu_part_range[I] = atom_mu_s.at(I - 1) + atom_mu_part_range[I - 1];
 
-    N_all_mu = atom_mu_part_range[natom - 1] + atom_mu[natom - 1];
+    N_all_mu = atom_mu_part_range[natom - 1] + atom_mu_s[natom - 1];
 
     return 0;
 }
diff --git a/qsgw/fermi_energy_occupation.cpp b/qsgw/fermi_energy_occupation.cpp
--- a/qsgw/fermi_energy_occupation.cpp
+++ b/qsgw/fermi_energy_occupation.cpp
@@ -158,12 +158,8 @@ double qsgw_physical_electron_count(
     const double tolerance)
 {
     validate_qsgw_kpoint_weights(meanfield, kpoint_weights, tolerance);
-    const double storage_capacity =
-        qsgw_state_capacity_factor(meanfield) /
-        static_cast<double>(meanfield.get_n_kpoints());
-    const double active_kpoint_count =
-        static_cast<double>(meanfield.get_n_kpoints());
+    const double capacity_factor = qsgw_state_capacity_factor(meanfield);
     double electrons = 0.0;
     for (int spin = 0; spin < meanfield.get_n_spins(); ++spin)
     {
         for (int kpoint = 0; kpoint < meanfield.get_n_kpoints(); ++kpoint)
         {
+            const double storage_capacity =
+                capacity_factor *
+                kpoint_weights[static_cast<std::size_t>(kpoint)];
             for (int band = 0; band < meanfield.get_n_bands(); ++band)
             {
                 const double stored =
@@ -175,8 +171,7 @@ double qsgw_physical_electron_count(
                     throw std::invalid_argument(
                         "Legacy Scheme-A occupation exceeds storage capacity");
                 }
-                electrons += stored * active_kpoint_count *
-                             kpoint_weights[static_cast<std::size_t>(kpoint)];
+                electrons += stored;
             }
         }
     }
@@ -212,9 +207,6 @@ QsgwZeroTemperatureOccupationResult update_qsgw_zero_temperature_occupations(
         meanfield, kpoint_weights, electron_tolerance);
     const double capacity_factor =
         qsgw_state_capacity_factor(reference_meanfield);
-    const double storage_capacity =
-        capacity_factor /
-        static_cast<double>(reference_meanfield.get_n_kpoints());
     const double reference_electrons = qsgw_physical_electron_count(
         reference_meanfield, kpoint_weights, electron_tolerance);
     std::vector<QsgwState> states;
@@ -238,7 +230,8 @@ QsgwZeroTemperatureOccupationResult update_qsgw_zero_temperature_occupations(
                         "Legacy Scheme-A eigenvalue is non-finite");
                 }
                 total_capacity += physical_capacity;
-                states.push_back({energy, physical_capacity, storage_capacity,
+                states.push_back({energy, physical_capacity,
+                                  physical_capacity,
                                   spin, kpoint, band});
             }
         }
@@ -308,25 +301,19 @@ QsgwZeroTemperatureOccupationResult update_qsgw_zero_temperature_occupations(
     double lowest = std::numeric_limits<double>::infinity();
     double highest = -std::numeric_limits<double>::infinity();
-    const double active_kpoint_count =
-        static_cast<double>(meanfield.get_n_kpoints());
     for (const QsgwState &state : states)
     {
         const double stored =
             updated_weights[state.spin](state.kpoint, state.band);
-        const double fraction = stored / state.storage_capacity;
-        const double physical_occupation = fraction * state.physical_capacity;
-        result.electron_count +=
-            stored * active_kpoint_count *
-            kpoint_weights[static_cast<std::size_t>(state.kpoint)];
+        result.electron_count += stored;
         if (state.physical_capacity <= electron_tolerance) continue;
         lowest = std::min(lowest, state.energy);
         highest = std::max(highest, state.energy);
-        if (physical_occupation > electron_tolerance)
+        if (stored > electron_tolerance)
             result.vbm = std::max(result.vbm, state.energy);
-        if (physical_occupation <
+        if (stored <
             state.physical_capacity - electron_tolerance)
             result.cbm = std::min(result.cbm, state.energy);
-        if (physical_occupation > electron_tolerance &&
-            physical_occupation <
+        if (stored > electron_tolerance &&
+            stored <
                 state.physical_capacity - electron_tolerance)
             result.metallic = true;
     }
diff --git a/src/librpa.cpp b/src/librpa.cpp
--- a/src/librpa.cpp
+++ b/src/librpa.cpp
@@ -264,7 +264,11 @@ void set_ao_basis_aux(int I, int J, int nbasis_i, int nbasis_j, int naux_mu, int
     {
         atom_nw.insert(pair<atom_t, size_t>(I, nbasis_i));
         atom_mu.insert(pair<atom_t, size_t>(I, naux_mu));
     }
+    else if (keyword == "Cs_shrinked_data")
+    {
+        atom_mu_s.insert(pair<atom_t, size_t>(I, naux_mu));
+    }
 
     if (insert_index_only)
     {
